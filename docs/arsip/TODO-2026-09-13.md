# Technical Debt Tracking — Les Ko Lui

> ⚠️ **BERKAS INI DIPINDAH KE `docs/arsip/TODO-2026-09-13.md` PADA 2026-09-13.**
> Alasannya: TODO di akar repo bersaing dengan `README.md` dan tidak pernah dibaca mesin pencari
> dokumentasi. **Daftar pekerjaan aktif sekarang ada di [`../README.md`](../README.md) §4**, dan
> pekerjaan refactor yang masih berjalan punya dokumen tugas sendiri di [`../kerja/`](../kerja/)
> ([`TASK-01-refactor-layar-besar.md`](../kerja/TASK-01-refactor-layar-besar.md); formatnya di
> [`TASK-02`](../kerja/TASK-02-format-dokumen-tugas-ai.md)).
> Isi di bawah dibekukan sebagai catatan historis.

Dibuat 2025-07-19 sebagai bagian dari refactoring round.
**Diperbarui 2026-09-13 (v1.75.0):** putaran refactor layar besar + test logika murni — lihat bagian "Putaran 2026-09-13".

## 🔵 Putaran 2026-09-13 — refactor layar besar + test logika (SELESAI)

Latar: `docs/README.md` §3 poin 3. Alasan utama dikerjakan: perubahan UI di layar-layar ini mahal
karena seluruh logika menumpuk di satu berkas, dan **tidak ada satu pun test** untuk logika tersebut
(toolchain belum punya React Testing Library → strateginya: keluarkan logika murni dari komponen,
lalu uji sebagai fungsi biasa).

| Layar | Sebelum | Sesudah | Yang dikeluarkan |
|---|---|---|---|
| `CaptureSession.tsx` | 2.677 | **2.591** | `captureSession/constants.ts` (86) + `captureSession/helpers.ts` (150) |
| `StudentDetail.tsx` | 1.429 | **1.039** | `studentDetail/RiwayatSesi.tsx` (230) + `studentDetail/IaEeTracker.tsx` (267) |
| `MonthlyReport.tsx` | 2.097 | 2.097 | belum disentuh putaran ini |

- [x] `captureSession/constants.ts` — durasi, chip situasi, label 12 indikator, metadata 6 langkah, `isValidStep`
- [x] `captureSession/helpers.ts` — `buildWaMessage`, `topicLevelHint`, `draftStamp`, `mergeTopics`/`splitTopics`, `mergeTopicUnits`, `recentTopics`, `appendSituasi`/`hasSituasi`
- [x] `studentDetail/RiwayatSesi.tsx` — grafik skor + daftar topik + daftar sesi (±190 baris JSX)
- [x] `studentDetail/IaEeTracker.tsx` — **11 `useState` + 4 fungsi repositori ikut pindah** (state itu tidak dipakai bagian lain layar)
- [x] `src/__tests__/captureSessionHelpers.test.ts` — **31 test** untuk logika yang tadinya tidak teruji
- [x] Dua bug ditemukan oleh test baru:
  - `draftStamp("bukan-tanggal")` mengembalikan `"Invalid Date"` (bukan kosong) karena `toLocaleString` tidak melempar → spanduk draf bisa berbunyi "tersimpan pada Invalid Date"
  - Ikon indikator `drowsy` bertabrakan dengan mood "Lelah" (🌙) — sisa audit P2 #15 yang terlewat
- Verifikasi: **561 test / 50 berkas lulus** (+31), `tsc -b` bersih, `eslint` 0/0, build produksi sukses

**Sisa di area yang sama (belum):**
- [ ] `CaptureSession.tsx` — ekstrak `useAiFill` (state + pemanggilan AI tersebar di 3 blok JSX) dan `CloseOutSheet` (±620 baris JSX)
- [ ] `MonthlyReport.tsx` (2.097) — `LayoutPicker` sengaja ditunda (§ di bawah); kandidat berikutnya: pisahkan blok pratinjau + toolbar desain ke `useDesignPicker`
- [ ] `Settings.tsx` (1.109) — pecah per seksi (Backup/PIN/Drive/Relay/Template/Storage)
- [ ] Hook layar masih tanpa test: `useCaptureDraft` (263), `useReportGeneration`, `useEngagement` (167) — butuh RTL atau ekstraksi lebih agresif

## 🟡 Screen Component Splits (In Progress)

Setiap screen component besar perlu dipecah menjadi sub-components dan custom hooks.

### StudentDetail.tsx (~82KB, ~1.490 baris saat ditulis; **kini 1.039 baris**, 6/7 seksi diekstrak)
- [x] Extract 6 sub-komponen seksi (lihat bagian bawah)
- [ ] Extract `useStudentData(studentId)` — fetch student + sessions + rapor + iaee
- [ ] Extract `useBilling(studentId)` — billing logic, month closing per student
- [ ] Extract `useAiInsights(studentId)` — AI summary generation
- [ ] Extract PIN verification into shared hook `usePinVerification`

### CaptureSession.tsx (~99KB, ~1.743 baris saat ditulis; **kini 2.591 baris** — bertambah karena P0–P3)
- [ ] Extract `useAiFill` — state AI + pemanggilan `draftShortNote`/`polishWhatsApp` (kini tersebar di 3 blok JSX)
- [ ] Extract `CloseOutSheet` — laporan sesi pasca-simpan (±620 baris JSX)
- [ ] Extract `useSessionForm()` — form state + validation
- [ ] Extract `useCamera()` — kamera/foto (sebagian sudah lewat `usePhotoCapture`)

### Settings.tsx (~1.109 baris)
- [ ] Split sections: `BackupSection`, `PinSection`, `DriveSection`, `RelaySection`, `TemplateSection`, `StorageSection`
- [ ] Extract `useStorageQuota()` — storage monitoring hook
- [ ] Extract `useBackup()` — backup/restore logic hook

### MonthlyReport.tsx (refactor v1.57.0: 2466 → ~1900 baris, +5 file di `screens/monthlyReport/`)
- [x] Extract `helpers.ts` — konstanta + fungsi murni (cleanText, buildSessionNarrative, normaliseAiPlan, dll.)
- [x] Extract `NextMonthPlanEditor.tsx` — editor rencana bulan depan (max 3 prioritas)
- [x] Extract `CustomThemeBuilder.tsx` — builder tema kustom + konstanta style
- [x] Extract `useReportExport.ts` — logika export JPG/PNG/PDF + tandai sudah dibagikan
- [x] Extract `useReportGeneration.ts` — seluruh logika AI (handlePolish, handleGenerateNarratives, fallback lokal) + state aiLoading/aiRequestRef/prevTexts
- [ ] Split `LayoutPicker` component — **sengaja ditunda**: toolbar desain butuh ~20 props (designOpen, undoStack, coverPage, showCompare, compareThemeId, showCustomBuilder, handleRegenerate, handleCreateOrSwitch, dll.) yang juga dipakai di luar toolbar (reportOptions, handleRegenerate), sehingga ekstraksinya hanya memindahkan JSX tanpa mengurangi coupling. Dipertimbangkan ulang bila state desain diangkat ke hook `useDesignPicker`.

## 🟢 Lower Priority

### Ketahanan Data — 3 Kriteria Tanpa Bukti Otomatis (dari `docs/04-RENCANA-KETAHANAN-DATA.md` §13)
- [ ] Tes hook `useReportGeneration`: respons AI gagal → `narrative`/`aiNarrativeHash` lama tetap utuh; retry sukses baru memperbarui
- [ ] Tes hook `useReportGeneration`: respons terlambat setelah ganti murid/periode tidak menimpa scope lain
- [ ] `Settings.tsx`: kirim hanya field yang diubah (dirty-field tracking) agar backup dari luar form tidak ter-rollback oleh snapshot `handleSave` yang basi
- [ ] `e2e-pwa`: uji dua build produksi berbeda (pembaruan antar-deploy, halaman lama membuka route lazy)
- [ ] Pertimbangkan menambah React Testing Library sebagai prasyarat tiga item pertama

### localStorage Usage Clarification
- [ ] Document which localStorage keys are "app meta" vs "domain data"
- [ ] Consider migrating relay config to IndexedDB settings table
- Current keys: `leskolui_last_auto_backup_prompt`, `leskolui_drive_auto`, `leskolui_drive_pass`, `leskolui_relay_secret`, changelog version, PIN lockout state

### Test Coverage for Screens
- [x] **Logika murni `CaptureSession`** — 31 test di `src/__tests__/captureSessionHelpers.test.ts` (2026-09-13): pesan WA (+ privasi), tingkat jenjang topik, gabung/pisah topik, bab topik, topik sesi lalu, chip situasi, validasi langkah, label & ikon indikator
- [ ] Hook layar (butuh RTL atau ekstraksi lebih agresif): `useCaptureDraft`, `useEngagement`, `useReportGeneration`, `useStudentBrief`
- [ ] Unit test untuk hook `StudentDetail`
- [ ] Unit test untuk logika penagihan `Payments`
- [ ] Integration test alur pengguna kunci

## ✅ Completed

- [x] Split repos.ts → `src/db/repos/` (9 domain files)
- [x] Split layouts.tsx → `src/template/layouts/` (helpers + 4 group files)
- [x] Fix empty catch blocks → `console.warn` added
- [x] Create `.env.example`
- [x] StudentDetail.tsx — 4 sub-components extracted: EvidenceCard, UpcomingSchedule, EngagementSummary, SessionDetailModal
- [x] CaptureSession.tsx — usePhotoCapture hook extracted (photo + signature lifecycle)
- [x] All 5 big screens — JSDoc @component headers added
- [x] TODO.md created with prioritized debt tracking
- [x] Dead code cleanup (2026-08-02): `exportAbsensi.ts`, `Popover.tsx`, `Gauge.tsx`, folder `captureSession/`, 10 export mati di barrel repos
- [x] `tsconfig.app.json` → `"strict": true` (2026-08-02, 0 error)
- [x] Audit v1.37.0 fixes (2026-08-02) — detail di `docs/arsip/AUDIT-CHECKLIST.md` Ronde 3: PIN lockout recovery, konsistensi uang deleteSession/closeMonth, PWA autoUpdate (fix stale chunk), a11y label+dialog
- [x] `Payments.tsx` dipecah jadi tab + hook (dilakukan sebelum 2026-09-13): `RingkasanTab` (751), `TagihanTab` (1.004), `PengeluaranTab`, `RekapTab`, `InvoiceModal`, `useInvoiceFilters`, `useInvoiceExports`, `useSessionCountBilling`
- [x] Refactor layar besar putaran 2026-09-13 — lihat bagian 🔵 di atas

## 🟡 Screen Component Splits (Partial — remaining tracked below)

### StudentDetail.tsx ✅ 6/7 sections extracted
- [x] Extract `EvidenceCard` — Bukti Keaktifan
- [x] Extract `UpcomingSchedule` — Jadwal Mendatang
- [x] Extract `EngagementSummary` — Keseriusan Belajar
- [x] Extract `SessionDetailModal` — Session detail bottom sheet
- [x] Extract `RiwayatSesi` — riwayat + grafik skor + daftar topik (2026-09-13)
- [x] Extract `IaEeTracker` — IA/EE/PP projects + milestone, termasuk state-nya (2026-09-13)
- [ ] Extract `NilaiRapor` — rapor grades section

### CaptureSession.tsx 🟢 3 hook + 2 modul diekstrak
- [x] Extract `usePhotoCapture` — camera + signature blob lifecycle
- [x] Extract `useStudentBrief` — load last session, HW, follow-ups on student change
- [x] Extract `useEngagement` — state indikator + derivasi skor + reset
- [x] Extract `captureSession/constants.ts` — durasi, chip situasi, label indikator, metadata langkah (2026-09-13)
- [x] Extract `captureSession/helpers.ts` — fungsi murni pesan WA/topik/situasi + 31 unit test (2026-09-13)
- [ ] Extract `useAiFill` — AI note/homework/WA generation + AiCostModal
- [ ] Extract `useCloseOut` — post-save homework/follow-up modal
